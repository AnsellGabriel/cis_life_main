<table class="table table-borderless table-hover">
  <thead>
    <tr class="border-top border-bottom border-primary">
      <%# <th class="text-primary">ID</th> %>
      <th>Claim Type</th>
      <th>
        <div class="text-end">
          <%= link_to '<i class="bi bi-plus-square"></i> Add Claim Type'.html_safe, new_claims_claim_type_agreement_path(v: @agreement), class: "btn btn-primary btn-sm", data: { turbo_frame: "modal" } if current_user.userable_type == "Employee" %></th>
      </div>
    </tr>
  </thead>
  <tbody>
    <% @agreement.claim_type_agreements.each do |a| %>
      <tr>
        <%# <td>= benefit_claim.id</td> %>
        <td> <%= a.claim_type %> </td>
        <td class="text-end">
          <%#= link_to 'View', benefit_claim, class: 'btn' %>
          <%#= link_to '<i class="bi bi-pencil-square"></i>'.html_safe, edit_claim_type_document_path(doc), class: 'btn btn-outline-primary btn-sm', data: { turbo_frame: "remote_modal" }  %>
          <%= link_to '<i class="bi bi-trash3"></i>'.html_safe, claims_claim_type_agreement_path(a),  data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
