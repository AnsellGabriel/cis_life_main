<div class="card">
  <div class="card-body">
    <%= render @claim_document %>
  </div>
  <div class="card-footer">
    <%= link_to "Edit", edit_claims_claim_document_path(@claim_document), class: 'btn btn-primary' %> |
    <%= link_to "Back", claims_claim_documents_url, class: 'btn btn-primary' %>
  </div>
</div>
