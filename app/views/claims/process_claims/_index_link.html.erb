<%# if p.submitted? || p.claim_filed? %>
<%#= link_to "Edit", claims_file_process_claim_path(p), class: "btn btn-warning btn-sm" if p.claim_filed? %>
<%#= link_to "View", show_coop_process_claim_path(p), class: "btn btn-warning btn-sm"  %>
<%# else %>
<%# if p.filed_by_analyst? %>
<%#= link_to "Edit", edit_ca_process_claim_path(p), class: "btn btn-warning btn-sm" %>
<%#= link_to "View", show_coop_process_claim_path(p), class: "btn btn-warning btn-sm"  %>
<%# else %>
<%#= link_to "View", claim_process_process_claim_path(p), class: "btn btn-warning btn-sm"  %>
<%# end %>
<%# end %>
<% if current_user.userable_type == 'CoopUser' %>
  <%= link_to "Edit", claims_file_process_claim_path(p), class: "btn btn-warning btn-sm" if p.claim_filed? %>
  <%= link_to "View", show_coop_claims_process_claim_path(p), class: "btn btn-warning btn-sm"  %>
<% else %>
  <%= link_to "Edit", edit_ca_claims_process_claim_path(p), class: "btn btn-warning btn-sm" %>
  <%#= link_to "View", show_coop_claims_process_claim_path(p), class: "btn btn-warning btn-sm"  %>
  <%= link_to "View", claim_process_claims_process_claim_path(p), class: "btn btn-warning btn-sm"  %>
<% end %>