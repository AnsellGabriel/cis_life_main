
<div class="<%= flex_justify_align('between', 'end') %> mb-1">
  <div class="">
    <div class="<%= flex_justify_align('', 'end') %> gap-1">
      <%= content_tag :h4, "Payment Types", class: 'text-primary mb-0' %>
      <%= link_to 'New', new_treasury_payment_type_path, class: small_btn('primary')%>
    </div>

    <% if @pagy.pages > 1 %>
      <div class="mt-3">
        <%== pagy_bootstrap_nav(@pagy) %>
      </div>
    <% end %>
  </div>

  <%= search_form_for @q do |f| %>
    <div class="d-flex gap-1">
      <%= f.search_field :name_cont, class: 'form-control', placeholder: 'Name' %>
      <%= f.submit 'Search', class: 'btn-sm btn btn-warning' %>
    </div>
  <% end %>
</div>

<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th class="" style="width: 10%">#</th>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
    <% @treasury_payment_types.each do |pt| %>
      <tr>
        <th scope="row"><%= pt.id %></th>
        <td>
          <%= pt.name %>
          <%= link_to edit_treasury_payment_type_path(pt), class: 'btn btn-outline-primary btn-sm', title: 'Edit' do %>
            <i class="bi bi-pencil"></i>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
