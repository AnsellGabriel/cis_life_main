<div class="p-5 mt-5 rounded-3">
  <div class="card shadow p-3 bg-body rounded border-0">
    <div class="row ">
      <div class="col-md-7 text-center">
        <iframe src="https://player.vimeo.com/video/945609139?h=30369b5e88" width="600" height="338" frameborder="0" allow="autoplay; fullscreen; picture-in-picture" allowfullscreen></iframe>
        <%# <iframe src="" width="640" height="564" frameborder="0" allow="autoplay; fullscreen" allowfullscreen></iframe> %>
      </div>
      <div class="col-md-5 ">
        <div class="text-center mb-3">
          <%= image_tag 'cis_logo.png', width: 150, class: '' %> <br>
          <strong class="fw-bold">Efficiency and Productivity</strong>
        </div>
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-inputs mb-3">
            <div class="form-floating mb-3">
              <%= f.input :email, 
                  wrapper: false,
                  label: false,
                  required: false,
                  input_html: { autocomplete: "email" },
                  placeholder: 'Email' %>
              <%= f.label :email, 'Email', class: 'ms-1', style: 'opacity: 0.5' %>
            </div>
            <div class="form-floating mb-3">
              <%= f.input :password, 
                  wrapper: false,
                  label: false,
                  required: false,
                  input_html: { autocomplete: "current-password" },
                  placeholder: 'Password' %>
              <%= f.label :password, 'Password', class: 'ms-1', style: 'opacity: 0.5' %>
            </div>
          </div>
          <div class="form-actions mb-3">
            <%= f.button :submit, "Sign in", class: "btn-primary w-100", style: 'background-color: #007bff !important; border-color: #007bff !important;', data: {turbo: false} %>
          </div>
        <% end %>
        <div class="text-center small">
          <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
            <%#= link_to "Forgot password?", new_password_path(resource_name), data: {turbo_frame: 'modal'}  %>
          <% end %>
        </div>
        <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
          <small class="text-muted"> Feeling new around here? No problem at all! <br>
            Click <%= link_to "Sign up", new_registration_path(resource_name), class: 'link-primary', data: {turbo_frame: 'modal-lg'} %> to join the gang and unlock all the perks.</small>
        <% end %>
      </div>
    </div>
  </div>
</div>
<div style="background-color: #f0f2f5">
  <div class="container">
    <div class="row">
      <div class="col-md-6">
        <%= image_tag 'demo_1' %>
      </div>
      <div class="col-md-6 ">
        <div class="">
          <div class="text-center mt-5">
            <p class=" fs-4">Let's Catch Up! Schedule a Meeting</p>
            <p>We get it - system can sometimes feel like navigating a maze. But don't worry, we're here to guide you through every step. Our team of experts is ready to give you the personalized attention you deserve. Just pick a time that suits you best, and we'll make sure one of our friendly faces is there to help you out.</p>
            <%= link_to 'Schedule My Meeting', new_demo_schedule_path, class: "btn btn-primary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
