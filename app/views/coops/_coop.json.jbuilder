json.extract! coop, :id, :last_name, :first_name, :middle_name, :birthdate, :mobile_number, :designation, :cooperative_id, :coop_branch_id, :created_at, :updated_at
json.url coop_url(coop, format: :json)
