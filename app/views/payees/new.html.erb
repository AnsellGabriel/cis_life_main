<%= link_to payees_path, class: 'btn btn-secondary mb-1' do %>
  <i class="bi bi-chevron-left"></i>
<% end %>

<div class="card" style="width: 500px">
  <h5 class="card-header">New Payee</h5>
  <div class="card-body">
    <%= render "form", payee: @payee %>
  </div>
</div>
