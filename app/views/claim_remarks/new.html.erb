<% if params[:s].blank? %>
  <% @title = "Add Claim Remarks" %>
<% else %>
  <% @title = params[:s] %>
<% end %>
<%= render "remote_modal", title: @title.to_s do %>
  <%= render "form", url: claim_remarks_path(v: @process_claim, s: @change_status), meth: 'post' %>
<% end %>