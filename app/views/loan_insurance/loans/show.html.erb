
<%= render @loan %>

<div>
  <%= link_to "Edit", edit_loan_insurance_loan_path(@loan), class: 'btn btn-primary' %> |
  <%= link_to "Back", loan_insurance_loans_path, class: 'btn btn-primary' %>

  <%= button_to "Delete", @loan, method: :delete, class: 'btn btn-danger' %>
</div>
