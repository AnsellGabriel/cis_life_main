<%= content_tag :h1, "List of Loan Insurance Batch Remits" %>


<div class="text-end mb-3">
  <%= link_to new_loan_insurance_batch_remit_path, class: 'btn btn-primary' do %>
    New Batch Remit
  <% end %>
</div>

<table class ="table table-striped">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>

      <th>#</th>
        <th></th>

      </tr>
    </thead>

    <tbody>
      <% @loan_insurance_batch_remits.each do |loan_insurance_batch_remit| %>
        <tr>

        <th scope="row"><%= loan_insurance_batch_remit.id %></th>
    
          <td>
            <%= link_to 'View', loan_insurance_batch_remit, class: 'btn btn-secondary btn-sm' %>
            <%= link_to 'Edit', edit_loan_insurance_batch_remit_path(loan_insurance_batch_remit), class: 'btn btn-secondary btn-sm' %>
            <%= link_to "Delete", loan_insurance_batch_remit, class: "btn btn-danger btn-sm", method: :delete, data: { "turbo-method": :delete, turbo_confirm: "Are you sure?" } %>
          </td>

        </tr>
      <% end %>
    </tbody>
  </table>
</div>
