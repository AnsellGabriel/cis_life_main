<% batches =  %>

<%= render 'agreement_field', 
	name: 'Approved Participants', 
	value: group_remit.batches.size, 
	margin: 'mb-2'
%>

<% unless @agreement.plan.acronym == 'PMFC' %>
	<%= render 'agreement_field', 
		name: 'Effectivity Date', 
		value: date_as_month_day_year(group_remit.effectivity_date), 
		margin: ''
	%>
	<%= render 'agreement_field', 
		name: 'Expiry Date', 
		value: date_as_month_day_year(group_remit.expiry_date), 
		margin: 'mb-2'
	%>
<% end %>

<%= render 'agreement_field', 
	name: 'Net Premium', 
	value: peso_currency(group_remit.coop_net_premium), 
	margin: ''
%>
<%= render 'agreement_field', 
	name: 'Cooperative Service Fee', 
	value: peso_currency(group_remit.total_coop_commissions), 
	margin: ''
%>
