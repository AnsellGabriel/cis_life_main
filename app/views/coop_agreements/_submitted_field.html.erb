<% batches =  %>

<%= render 'agreement_field', 
	name: 'Enrolled members', 
	value: group_remit.batches.size, 
	margin: 'mb-2'
%>

<% unless @agreement.plan.acronym == 'PMFC' %>
	<%= render 'agreement_field', 
		name: 'Effectivity Date', 
		value: to_shortdate(group_remit.effectivity_date), 
		margin: ''
	%>
	<%= render 'agreement_field', 
		name: 'Expiry Date', 
		value: to_shortdate(group_remit.expiry_date), 
		margin: 'mb-2'
	%>
<% end %>
