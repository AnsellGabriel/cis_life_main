:scheduler:
  :schedule:
    group_remit_update:
      cron: '13 14 * * *' # Set the desired time for the job to run (4:00 am in this example)
      class: 'GroupRemitStatusUpdater'
      queue: 'default'
